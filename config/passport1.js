// var passport = require('passport');
// var localStrategy = require('passport-local').Strategy;
// var db = require('../models');

// passport.use(new localStrategy({usernameField: 'email'}, function(email)))
